/** @format */

import React from "react";
import { StyledFormWrapper } from "./style";

export default function FormWrapper(props) {
  return <StyledFormWrapper>{props.children}</StyledFormWrapper>;
}
